@model IEnumerable<Model.Models.Dialogue>

@{
    ViewBag.Title = "Lista";
}

<h2>List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DialogueId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DialogueId }) |
            @Html.ActionLink("Delete", "Remove", new { id=item.DialogueId })
        </td>
    </tr>
}

</table>
