@model IEnumerable<Model.Models.Actor>

<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Nazwa")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.Display("Nazwa")
        </td>
        <td>
            @Html.ActionLink(" ", "Edit", "Actor", new { idActor = item.ActorId }, new { @class = "glyphicon glyphicon-edit" }) |
            @Html.ActionLink(" ","Delete", "Actor", new { idActor=item.ActorId, idDialogue = item.DialogueId }, new { @class = "glyphicon glyphicon-trash" })
        </td>
    </tr>
}
</table>
